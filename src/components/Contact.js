import React from 'react';

// Contact info has been consolidated into the footer to avoid redundancy.
// This component intentionally renders null so navigation to #contact
// does not duplicate contact details. If you'd rather keep a small
// anchor section, let me know and I can render a minimal placeholder.
export default function Contact() {
  return null;
}